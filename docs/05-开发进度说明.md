# VoFlow Platform 开发进度说明

生成日期：2026-06-26
依据文件：

- `.kiro/plans/voflow-platform/plan.md`
- `.kiro/specs/*/tasks.md`

## 1. 当前结论

当前开发已经完成 Batch 3 的 `voflow-video-render`、`voflow-advanced-editing`、`voflow-packaging-export` checkpoint，以及 Batch 4 的 `voflow-publish-assistant` checkpoint。数字人口播中间视频链路已达到 mock/local adapter 级验收，高级剪辑配置/UI/轻量预览已完成，包装导出链路已完成到 mock E2E、真实媒体 MP4 smoke 和失败重试自动化验收层面，发布辅助链路已完成到 mock OAuth/mock Channel Adapter MVP 验收层面。现已新增 `voflow-real-platform-publish` Spec，用于承接真实平台 OAuth/Adapter 接入、发布状态同步和最终 MVP 环境复验。

当前唯一开发入口为：

- Active Spec：`voflow-real-platform-publish`
- Active Scope：`voflow-real-platform-publish` Task 0-8 已完成，已确认当前唯一开发入口、锁定首个真实平台为 `youtube_shorts` / YouTube Data API v3，并完成真实发布配置、真实 OAuth、token refresh、final_video resolver、YouTube Adapter 和手动状态同步 API
- Active Focus：`voflow-real-platform-publish` Task 9：更新发布中心真实状态 UI
- Status：`task_8_completed_ready_for_task_9`

从任务勾选状态看，`voflow-video-render` 已完成 Task 0-12，共 13/13 项；`voflow-advanced-editing` 已完成 Task 0-10，共 11/11 项；`voflow-packaging-export` 已完成 Task 0-16，共 17/17 项；`voflow-publish-assistant` 已完成 Task 0-14，共 15/15 项；`voflow-real-platform-publish` 已完成 Task 0-8，共 9/13 项。第 12、13、14 条 MVP 主线仍保持 `partial`，不能把真实模型、生产字幕硬烧、复杂剪辑 filter 或真实平台发布当作已最终复验。

一句话概括：项目基础能力、素材合规、参考提取、文案 AI、法务审查、TTS、自拍数字人、数字人渲染主体链路、高级剪辑配置链路、包装导出链路和发布辅助链路已经完成到自动化验收层面；真实平台发布后端已推进到 YouTube OAuth/Adapter/状态同步的 HTTP mock 自动化验收层面；真实 MuseTalk/SadTalker 服务、生产 FFmpeg 字幕硬烧、复杂剪辑 filter、发布中心真实状态 UI、真实 YouTube 账号联调和最终 MVP 环境复验仍未完成。

## 2. MVP 主线进度

按 `plan.md` 的 MVP 验收主线矩阵统计，当前主线状态如下：

| 状态 | 数量 | 说明 |
| --- | ---: | --- |
| done | 9 | 登录、项目创建、照片质检/授权、参考素材/参考链接提取、文案生成、法务审查等已完成，并有测试或验收记录 |
| partial | 7 | 我的数字人真实预览、克隆音色真实训练、数字人视频渲染、高级剪辑底层配置/轻量预览/UI、导出请求数据表、SRT/ASS subtitle artifact 服务、BGM 授权/混音、cover artifact 服务、final_export Worker、ffprobe 媒体验收、导出 API、下载 API、导出 UI、发布相关数据表、发布平台规则配置、发布草稿生成和查询/编辑 API/service、渠道账号状态展示 API、token 加密存储 helper、OAuth/mock 授权占位接口、发布参数检查 API、mock Channel Adapter、一键发布 API、失败重试 API、发布信息编辑 UI、任务中心后续节点联调仍需补齐或复验 |
| not_started | 0 | 当前 MVP 主线均已至少启动；真实平台发布已推进到后端 OAuth/Adapter/状态同步 HTTP mock 自动化验收层面 |
| blocked | 0 | plan 未记录当前存在 blocked 状态 |

需要注意：`done` 不等于最终上线完成。plan 中多处仍要求在最终 MVP 环境做复验，尤其是依赖真实本地模型、对象存储、FFmpeg、Worker 的链路。

## 3. Spec 级别进度

| Spec | plan 状态 | tasks 勾选 | 当前判断 | 下一步 |
| --- | --- | ---: | --- | --- |
| `voflow-foundation` | done | 24/24 | 平台底座已完成 | 最终 MVP 环境复验 |
| `voflow-assets-compliance` | done | 15/15 | 素材上传、对象存储、授权、审计已完成 | 最终 MVP 环境复验 |
| `voflow-workflow-engine` | partial | 17/17 | 任务引擎基础完成，但还要和后续节点端到端联调 | 跟随 avatar render/export/publish 做联调 |
| `voflow-local-model-monitor` | done | 10/10 | 本地模型服务配置和健康检查基础完成 | 后续真实模型接入时复用 |
| `voflow-reference-extract` | done | 10/10 | 上传参考素材提取完成 | 最终 MVP 环境复验 |
| `voflow-reference-url-import` | done | 15/15 | 公开参考链接 metadata/字幕/授权音频导入完成 | 最终 MVP 环境复验 |
| `voflow-script-ai` | done | 19/19 | 文案改写、标题生成、风险检查完成 | 最终 MVP 环境复验 |
| `voflow-legal-review` | done | 12/12 | AI 法务审查和 TTS 前阻断完成 | 最终 MVP 环境复验 |
| `voflow-tts` | done | 15/15 | 预置音色 TTS、试听、确认链路完成 | 与 voice clone 和 avatar render 联调 |
| `voflow-voice-clone` | partial | 12/12 | 任务已全部勾选，但真实 GPT-SoVITS/CosyVoice trainer 尚未在 checkpoint 人工联调 | 真实 trainer 环境复验后才能升级为 done |
| `voflow-self-avatar` | done | 22/22 | 照片质检、授权、我的数字人管理已完成 | 最终 MVP 环境复验 |
| `voflow-video-render` | partial | 13/13 | Spec checklist 已完成；真实 MuseTalk/SadTalker 服务仍需最终环境复验 | 最终 MVP 环境复验真实模型服务 |
| `voflow-advanced-editing` | partial | 11/11 | Task 0-10 全部完成 checkpoint；当前预览为轻量配置 artifact，不是真实 FFmpeg 低清 MP4 | 等 packaging-export 做真实成片合成 |
| `voflow-packaging-export` | partial | 17/17 | Task 0-16 已完成，已建立 `export_requests` 表、SRT/ASS subtitle artifact 服务、BGM 授权/混音、cover artifact 服务、final_export Worker、ffprobe 媒体验收、导出 API、下载 API、导出 UI、mock provider 端到端脚本、真实媒体 MP4 smoke、失败重试测试和 checkpoint | 最终环境复验真实字幕硬烧和复杂剪辑 filter |
| `voflow-publish-assistant` | partial | 15/15 | Task 0-14 已完成 checkpoint；已新增发布相关数据表和 Prisma enum、平台规则模块、本地 OpenAI-compatible 发布草稿 provider、草稿生成/查询/编辑 API、渠道账号状态 API、token 加密 helper、mock OAuth 授权入口、发布参数检查 API、mock Channel Adapter、一键发布 API、失败重试 API、发布信息编辑 UI、发布中心操作台 UI、发布辅助 acceptance 测试和 checkpoint 验收记录；15 个 publish focused test 文件 60 个测试、lint、build、diff check 均通过 | 最终 MVP 环境复验或真实平台 OAuth/Adapter 接入 |
| `voflow-real-platform-publish` | partial | 9/13 | Task 0-8 已完成，已确认当前唯一开发入口、上游 publish-assistant mock checkpoint 边界，并新增 `docs/06-真实平台OAuth-Adapter接入方案.md`；首个真实平台锁定为 `youtube_shorts` / YouTube Data API v3；已新增真实发布集中配置、真实 OAuth callback/refresh、真实 token 数据模型、final_video resolver、YouTube resumable upload Adapter、远端状态同步 API；publish/channel focused suite 18 个文件 73 个测试、Prisma validate/migrate status、lint、build、diff check 通过 | 下一步执行 Task 9，更新发布中心真实状态 UI |

## 4. 当前正在做的能力

当前正在推进 `voflow-real-platform-publish`：

- Task 0 已完成：后续开发规范检查，确认当前唯一开发入口、读取本 Spec 和上游 `voflow-publish-assistant` 文档，并明确真实平台 endpoint、scope、密钥、token、发布状态文案、错误码和重试策略不得散写。
- Task 1 已完成：调研并锁定首个真实平台为 `youtube_shorts` / YouTube Data API v3，已记录官方 OAuth、上传、发布、状态查询、scope、审核和回调要求，并输出真实平台接入方案文档。
- Task 2 已完成：新增真实发布集中配置、YouTube OAuth/API/upload/scope/env 示例、`PUBLISH_REAL_PLATFORM_NOT_CONFIGURED` 稳定错误码和禁用 mock fallback 时的 fail-fast Adapter 入口，避免生产真实发布静默回退 mock。
- Task 3 已完成：扩展 `channel_accounts` 和 `publishes`，补齐真实 OAuth token、scope、provider account、metadata、refresh/error、remoteUrl、remoteStatus、lastSyncedAt、attemptsJson 字段，并保持 mock channel account 兼容。
- Task 4 已完成：实现 YouTube 真实授权 URL、HMAC signed state、callback code exchange、频道 profile 拉取和加密保存 token/metadata。
- Task 5 已完成：实现 refresh token 解密刷新、刷新失败稳定错误码和脱敏错误处理，新增 refresh API。
- Task 6 已完成：实现 final_video artifact 当前 team/job 校验和对象存储 stream 解析，并将一键发布接入真实 adapter registry。
- Task 7 已完成：实现 YouTube resumable upload Adapter，HTTP mock 覆盖 upload session、MP4 PUT、remoteId、remoteUrl 和远端状态映射。
- Task 8 已完成：实现手动刷新发布状态 API，保存 remoteStatus、lastSyncedAt、remoteUrl；既有失败重试会在 token 失效或参数检查失败时阻断盲目重试。
- Task 9-12 待执行：更新发布中心真实状态 UI、补齐真实接入自动化测试、执行最终 MVP 环境复验并完成 checkpoint。

上一阶段 `voflow-publish-assistant` 已完成：

- Task 0 已完成：确认平台规则、发布草稿、渠道账号、发布校验、Channel Adapter 和 API 响应必须进入公共 helper/service，不能散写在 API、Adapter 或 UI 中。
- Task 1 已完成：新增 `channel_accounts`、`publish_drafts`、`publishes` 数据表。
- Task 1 已完成：新增 `PublishPlatform`、`ChannelAccountStatus`、`PublishStatus` Prisma enum，收口发布平台、渠道账号状态和发布状态。
- Task 1 已完成：新增 schema 测试覆盖表、列、enum、外键、关键索引，并完成 RED/GREEN。
- Task 1 已完成：`prisma format`、`prisma validate`、`prisma migrate deploy`、`prisma generate`、publish schema test 和 export schema 回归测试均通过。
- Task 2 已完成：新增 `src/lib/publish/rules.ts`，集中配置 7 个发布平台的标题长度、标签数量、封面比例和视频时长限制。
- Task 2 已完成：新增 `tests/publish-platform-rules.test.ts`，覆盖平台顺序、规则完整性、关键平台边界值和未知平台错误，并完成 RED/GREEN。
- Task 3 已完成：新增本地 OpenAI-compatible 发布草稿 LLM provider，支持解析纯 JSON 和 fenced JSON。
- Task 3 已完成：新增 `generatePublishDrafts()` service，从 approved script 生成 title、description、tags、topics，并 upsert 到 `publish_drafts`。
- Task 3 已完成：新增 `POST /api/video-jobs/[jobId]/publish-drafts/generate`，支持鉴权、平台请求体验证、404 和发布草稿生成成功响应。
- Task 4 已完成：新增 `GET /api/video-jobs/[jobId]/publish-drafts`，支持按当前 team/job 查询平台草稿列表。
- Task 4 已完成：新增 `PUT /api/publish-drafts/[draftId]`，支持修改标题、描述、标签、话题和封面 artifact。
- Task 4 已完成：新增发布草稿 serializer/service，统一输出 tags/topics/rule/date 字段，保存时复用平台规则校验标题长度、标签数量，并校验封面 artifact 属于同一个 job 且类型为 `cover`。
- Task 5 已完成：新增 `GET /api/channel-accounts`，返回所有发布平台的账号授权展示状态。
- Task 5 已完成：新增渠道账号 serializer/service，支持 connected、expired、not_connected 展示口径，返回账号昵称和过期时间，过期 token 自动展示为 expired。
- Task 5 已完成：新增 AES-256-GCM 渠道 token 加密 helper 和 `saveChannelAccountToken()`，并在 `.env.example` 增加 `CHANNEL_TOKEN_ENCRYPTION_SECRET` 示例。
- Task 6 已完成：新增 `GET /api/channel-accounts/[platform]/authorize`，返回 mock OAuth 授权/重新授权入口。
- Task 6 已完成：新增 `POST /api/channel-accounts/[platform]/mock-authorize`，复用 token 加密 helper 创建或覆盖 connected mock channel account。
- Task 6 已完成：新增 OAuth mock provider 公共模块和 `ChannelOAuthAdapter` 接口占位，明确真实平台 OAuth 和发布 Channel Adapter 尚未实现。
- Task 7 已完成：新增 `POST /api/video-jobs/[jobId]/publish/validate`，支持按平台执行发布前检查。
- Task 7 已完成：新增发布检查公共类型和 `validatePublishParameters()` service，校验标题长度、标签数量、封面比例、最终视频是否存在、视频比例、视频时长和 channel account 状态。
- Task 7 已完成：检查结果会写回 `publish_drafts.validationJson`，失败时返回 `PUBLISH_VALIDATION_FAILED` 和逐项原因。
- Task 8 已完成：新增 `PublishChannelAdapter` 接口，包含 `uploadVideo()`、`publish()`、`getStatus()`、`retry()`。
- Task 8 已完成：新增 mock channel adapter，返回稳定的 mock request id、remote video id、remote publish id 和 published 状态。
- Task 8 已完成：新增 Adapter 错误码和结果类型，为 Task 9 一键发布 API、Task 10 失败重试 API 复用；当前不代表真实平台发布能力。
- Task 9 已完成：新增 `POST /api/video-jobs/[jobId]/publish`，只对检查通过且 connected 的平台调用 mock adapter 创建发布记录。
- Task 9 已完成：一键发布会保存 `publishes.requestId`、`publishes.remoteId` 和平台结果；expired 或 not_connected 平台会写入 `skipped` 并保存稳定原因。
- Task 9 已完成：新增 publish serializer/helper 和 `createPublishesForJob()` service；route 只负责鉴权、请求体验证和错误映射。
- Task 10 已完成：新增 `POST /api/publishes/[publishId]/retry`，只允许 failed 发布记录重试。
- Task 10 已完成：失败重试复用原 publish draft、channel account、validationJson 和 mock channel adapter 的 `retry()`，成功后更新同一条 publish 记录的新 `requestId`、`remoteId`。
- Task 10 已完成：新增错误历史格式，原失败 request/remote id 和失败原因会写入 `errorJson.history`，本次重试信息写入 `lastRetry`。
- Task 11 已完成：新增发布信息编辑 UI，`/dashboard/publish` 不再是占位页。
- Task 11 已完成：新增 `PublishDraftEditor`，支持平台 tabs、标题、标签、描述、话题和封面 Artifact ID 编辑。
- Task 11 已完成：标题限制、标签数量、封面推荐比例、封面允许比例和视频时长提示来自 `draft.rule`，未在页面散写平台规则。
- Task 12 已完成：新增发布中心操作台 `PublishCenterPanel`，展示账号授权状态、发布参数检查、发布动作和发布日志。
- Task 12 已完成：`/dashboard/publish` 接入账号刷新、mock 授权、发布前检查、一键发布、failed 单平台重试和仅导出 MP4 入口。
- Task 12 已完成：发布日志展示一键发布/重试返回的 status、requestId、remoteId、错误原因和重试按钮；当前不新增发布历史查询 API。
- Task 12 已完成：发布状态 label、动作 label、检查摘要 helper 已收口到 `src/lib/publish/ui.ts`，未在页面散写平台列表或发布状态文案。
- Task 13 已完成：新增 `tests/publish-assistant-acceptance.test.ts`，覆盖平台草稿生成、标题超长阻断发布、token 过期平台 skipped、部分平台失败后单独重试。
- Task 13 已完成：publish focused suite 扩展为 15 个测试文件、60 个测试；新增测试只验证既有能力，未修改生产代码。
- Task 14 已完成：发布辅助 checkpoint 已逐条对照 US-1 到 US-4 验收，15 个 publish focused test 文件 60 个测试通过，`next lint`、`git diff --check`、`next build` 通过。
- Task 14 已完成：`tsc --noEmit` 仍因既有测试类型债失败，发布助手相关文件和 Task 13 新增测试不在错误列表中。
- Task 14 已完成：`/dashboard/publish` 本地路由作为受保护页面返回登录重定向；发布中心 UI 和发布相关 API 已进入 build route 列表。
- Task 14 已完成：明确 mock OAuth/mock Channel Adapter 边界，不能按真实平台发布能力验收。

当前剩余实现点：

- 当前 publish-assistant checklist 已完成，真实平台 OAuth/Adapter 接入、发布状态同步增强和最终 MVP 环境复验已转入 `voflow-real-platform-publish`。
- 定时发布后端调度和发布历史查询 API 未列为真实平台接入首要闭环，若执行中确认必须进入 MVP，应先更新 `voflow-real-platform-publish` 的 Change Log。

## 5. 后续未完成范围

后续还剩三个主要方向，其中 `voflow-advanced-editing` 已完成底层配置、查询/保存 API、画中画/背景素材授权校验、关键词高亮配置生成、轻量预览 Worker 和视频剪辑 UI：

1. `voflow-advanced-editing`
   - 字幕样式
   - 画中画
   - 背景素材
   - 片头片尾
   - 转场
   - 剪辑预览

2. `voflow-packaging-export`
   - 导出请求数据表已完成
   - SRT/ASS 字幕生成服务已完成
   - BGM 授权校验已完成
   - BGM 混音服务已完成
   - 封面抽帧已完成
   - 封面标题合成已完成
   - Export Worker 已完成基础路径
   - ffprobe 媒体验收已完成
   - 导出 API 已完成
   - 下载 API 已完成
   - 导出 UI 已完成
   - mock provider 端到端脚本已完成
   - 真实媒体 MP4 smoke 已完成
   - 失败重试测试已完成
   - MVP 成片 checkpoint 已完成

3. `voflow-publish-assistant`
   - 后续开发规范检查已完成
   - 发布相关数据表已完成
   - 平台规则配置已完成
   - 发布标题、标签、描述、话题生成已完成
   - 发布草稿查询和编辑 API 已完成
   - 渠道账号状态已完成
   - token 加密存储 helper 已完成
   - OAuth/授权占位接口已完成
   - 发布参数检查已完成
   - Channel Adapter 接口已完成，当前为 mock adapter
   - 一键发布 API 已完成，当前复用 mock adapter
   - 失败重试 API 已完成，当前复用 mock adapter
   - 发布信息编辑 UI 已完成
   - 发布中心 UI 已完成
   - 测试补齐已完成
   - 发布辅助 checkpoint 已完成

4. `voflow-real-platform-publish`
   - Spec 已创建
   - 后续开发规范检查已完成
   - 真实平台接入决策已完成，首个平台锁定 `youtube_shorts` / YouTube Data API v3
   - 真实发布配置和 fail-fast 校验已完成
   - 渠道账号 token 数据模型扩展已完成
   - OAuth authorize/callback/refresh 已完成
   - final_video artifact 解析和 Adapter registry 已完成
   - 首个平台 YouTube Channel Adapter 已完成 HTTP mock 自动化验收
   - 发布状态手动同步和失败重试前置阻断已完成
   - 发布中心真实状态 UI 待执行
   - 后台状态同步 worker/cron 待增强
   - 最终 MVP 环境复验待执行

## 6. 风险和待复验点

当前进度里的主要风险不是任务数量，而是真实环境复验和发布闭环尚未完成：

- `voflow-voice-clone` 虽然 tasks 已 12/12 完成，但真实 GPT-SoVITS/CosyVoice trainer 没有在 checkpoint 人工联调，因此 plan 仍标为 `partial`。
- `voflow-video-render` 依赖真实 MuseTalk/SadTalker 或等价本地 avatar 服务。mock/provider 流程和自动化测试已经覆盖，但真实模型服务仍需最终环境复验。
- `voflow-workflow-engine` 的基础任务已完成，但任务中心必须继续和 reference_url_import、voice_clone、avatar_render、export、publish 节点做端到端联调，因此仍是 `partial`。
- `voflow-advanced-editing` 已完成底层配置表、查询 API、保存 API、画中画素材授权校验、背景素材授权校验、关键词高亮配置生成、轻量预览 Worker、视频剪辑 UI、测试补齐和 checkpoint；`voflow-packaging-export` 已完成导出请求数据表、SRT/ASS subtitle artifact 服务、BGM 授权/混音、封面 artifact 服务、final_export Worker 基础路径、ffprobe 媒体验收、导出 API、下载 API、导出 UI、mock provider 端到端脚本、真实媒体 MP4 smoke、失败重试测试和 checkpoint，但真实 MuseTalk/SadTalker 数字人模型、生产 FFmpeg subtitles filter 字幕硬烧、复杂画中画/背景/转场 filter 仍需最终环境联调增强。
- plan 已新增 `voflow-real-platform-publish`，Task 0-8 已完成；下一步必须先更新发布中心真实状态 UI，再补齐剩余真实接入测试和最终 MVP 环境复验。

## 7. 建议下一步

下一步已经进入新的 Kiro checklist。建议执行：

```text
voflow-real-platform-publish Task 9: 更新发布中心真实状态 UI
```

建议启动顺序：

1. 执行 `voflow-real-platform-publish` Task 9，在发布中心展示真实 provider、账号昵称、scope、过期时间、remoteUrl、remoteStatus、requestId、remoteId、lastSyncedAt 和重新授权/刷新状态入口。
2. Task 10 补齐 UI 状态测试、更多 OAuth/refresh/adapter/status sync 测试和 token 脱敏回归。
3. Task 11 执行最终 MVP 环境复验，重点复验真实 YouTube OAuth、真实上传、平台侧 remoteId/remoteUrl/remoteStatus 可查，以及前序 final_video MP4。
4. Task 12 完成真实平台发布与最终 MVP checkpoint，不能把 HTTP mock 证据当作真实账号环境复验证据。
5. 在最终 checkpoint 前，清理或登记 `tsc --noEmit` 暴露的既有测试类型债。
